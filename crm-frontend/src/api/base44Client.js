/**
 * Base44 SDK retired from runtime — platform-required stub.
 *
 * AuthContext.jsx is a platform-managed file that must import { base44 }
 * from '@/api/base44Client'. This stub satisfies that import requirement
 * WITHOUT importing @base44/sdk. The exported object is a no-op; all
 * authentication and data operations use the Railway API exclusively
 * (src/api/railway/client.js → /api/v1/*).
 *
 * Zero @base44/sdk imports. Zero Base44 runtime dependencies.
 */
export const base44 = {};

export default base44;
